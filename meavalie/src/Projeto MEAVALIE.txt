                         ┌─────────────────────────┐
                         │        VEICULO          │
                         ├─────────────────────────┤
                         │ - id: Long              │
                         │ - nome: String          │
                         │ - marca: String         │
                         │ - modelo: String        │
                         │ - ano: Integer          │
                         │ - capacidade: Integer   │
                         │ - ativo: Boolean        │
                         ├─────────────────────────┤
                         │ + getId(): Long         │
                         │ + getNome(): String     │
                         │ + getMarca(): String    │
                         │ + getModelo(): String   │
                         └───────────△─────────────┘
                                     │
                     ┌───────────────┴───────────────┐
                     │                               │
          ┌──────────┴───────────┐       ┌───────────┴───────────┐
          │        BARCO         │       │         BUGRE         │
          ├──────────────────────┤       ├───────────────────────┤
          │ - id: Long          │       │ - id: Long            │
          │ - tipoBarco: String │       │ - tipoTerreno: String │
          │ - comprimento: Double│      │ - tipoMotor: String   │
          │ - possuiCobertura:  │       │ - quantidadeLugares:  │
          │   Boolean            │       │   Integer             │
          │ - quantidadeMotores:│       │ - possuiCapota:       │
          │   Integer            │       │   Boolean             │
          ├──────────────────────┤       ├───────────────────────┤
          │ + navegar(): void   │       │ + dirigir(): void     │
          └──────────────────────┘       └───────────────────────┘













┌─────────────────────────────────────┐
│          AVALIACAO_PASSEIO          │
├─────────────────────────────────────┤
│ - id: Long                          │
│ - seguranca: Integer                │
│ - conforto: Integer                 │
│ - motorista: Integer                │
│ - qualidadeVeiculo: Integer         │
│ - qualidadePasseio: Integer         │
│ - atendimento: Integer              │
│ - pontualidade: Integer             │
│ - comentario: String                │
│ - dataAvaliacao: LocalDateTime      │
├─────────────────────────────────────┤
│ + calcularMedia(): Double           │
│ + validarNota(): Boolean            │
└─────────────────────────────────────┘

@Entity
public class AvaliacaoPasseio {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    private Integer seguranca;          // 0 a 10
    private Integer conforto;           // 0 a 10
    private Integer motorista;          // 0 a 10
    private Integer qualidadeVeiculo;   // 0 a 10
    private Integer qualidadePasseio;   // 0 a 10
    private Integer atendimento;        // 0 a 10
    private Integer pontualidade;       // 0 a 10

    private String comentario;

    private LocalDateTime dataAvaliacao;


}